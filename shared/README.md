# Shared Classes

Common classes reused across Problems 1, 2, and 3, for example:
- Location.java (represents a node/point, e.g. a campus building or city stop)
- Graph.java (represents the weighted graph/map used across problems)

Keeping these here avoids duplicating the same data model three times.
